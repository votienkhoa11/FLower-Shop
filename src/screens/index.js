import {View, Text} from 'react-native';
import React from 'react';

const index = () => {
  return (
    <View>
      <Text>Each screens folder will be added when there is a project UI design.</Text>
    </View>
  );
};

export default index;
